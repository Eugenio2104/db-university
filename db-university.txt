
1
Dentro la tabella students

SELECT `name`,`suraname`, `date_of_birth`
FROM `students`
WHERE YEAR (`date_of_birth`) = 1990
ORDER BY `date_of_birth`;

2
Dentro la tabella courses

SELECT `name`, `cfu` 
FROM `courses` 
WHERE `cfu` > 10 
ORDER BY `cfu`;

3
SELECT `name`, `surname`, `date_of_birth`
FROM `students`
WHERE YEAR(date_of_birth) < YEAR(NOW()) - 30
ORDER BY `date_of_birth`;

